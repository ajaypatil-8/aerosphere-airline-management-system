# Copy this file to .env and fill in your actual values
# cp .env.example .env
#hey i am ajay and this is not the orignal .env file this is just for demo/example purpose so if you want to run the project
#then you have to create your own .env file and fill in the actual values for database, razorpay, smtp, and sms api keys.

MYSQL_ROOT_PASSWORD=change_me_strong_password
MYSQL_DATABASE=airlinedb
MYSQL_USER=aerosphere
MYSQL_PASSWORD=change_me_strong_password

DB_URL=jdbc:mysql://mysql:3306/airlinedb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DB_USER=aerosphere
DB_PASSWORD=change_me_strong_password

RAZORPAY_KEY_ID=rzp_test_your_key_id
RAZORPAY_KEY_SECRET=your_razorpay_secret

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_gmail@gmail.com
SMTP_PASSWORD=your_gmail_app_password
SMTP_FROM=AeroSphere

SMS_API_KEY=your_fast2sms_api_key

APP_NAME=AeroSphere
APP_BASE_URL=http://localhost

APP_PORT=8080
NGINX_PORT=80
